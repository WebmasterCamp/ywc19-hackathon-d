import { atom } from 'jotai'

/**
 * cost before giving birth to the child
 */
export const costBeforeBirth = atom(0)

/**
 * cost before the child entering prepreschool
 */
export const costBeforePrePreSchool = atom(0)

/**
 * cost before the child entering preschool
 */
export const costBeforePreschool = atom(0)

/**
 * cost before the child entering primary school (primary school section doesn't exist for our system)
 */
export const costBeforePrimarySchool = atom(0)
